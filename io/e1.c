#include <stdio.h> //standerd input output library, included printf() and scanf()

int main()	//This function must exist for the script to be valid.
{
	printf("Hello world"); //Prints  the sring
	return 0; //The function type was int, so an interger must be returned
}

